allprojects {
    apply plugin: 'java-library'
    apply plugin: 'maven-publish'
    group = maven_group

    targetCompatibility = sourceCompatibility = JavaVersion.VERSION_20

    repositories {
        mavenLocal()
        mavenCentral()
    }
}